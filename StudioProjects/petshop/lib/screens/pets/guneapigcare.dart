import 'package:flutter/material.dart';

class GuineaCarePage extends StatelessWidget {
  const GuineaCarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(
          child: const Column(
            children: [
              Text("")
            ],
          ),
        ),
        
      ),
    );
  }
}